"""Vendored so this repository runs standalone.

Both modules here originate in a private operations codebase. They are copied
rather than imported so that reproducing the study needs nothing beyond this
repository, Python and Ollama — and so the tool schemas stay pinned to exactly
what the recorded trials saw.
"""
